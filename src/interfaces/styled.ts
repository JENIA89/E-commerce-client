export interface ITheme {
  colors: {
    footerBG: string;
    green: string;
    yellow: string;
    silver: string;
    grey: string;
    white: string;
  };

  sizes: {
    container: { maxWidth: number };
  };
}
